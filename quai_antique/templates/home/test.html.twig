{% extends 'base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #333;
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
        }
        .box {
            padding: 15px;
            margin: 15px 0;
            border-radius: 5px;
        }
        .success {
            background-color: #d4edda;
            color: #155724;
        }
        .nav {
            margin: 20px 0;
        }
        .nav a {
            display: inline-block;
            padding: 10px 15px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ page_title }}</h1>
        
        <div class="box success">
            <p>✅ Si vous voyez cette page, cela signifie que votre installation Symfony fonctionne correctement!</p>
            <p>✅ Les routes sont configurées correctement</p>
            <p>✅ Le système de templates Twig fonctionne</p>
        </div>
        
        <p>Cette page est créée pour vérifier que le routage fonctionne dans votre application Symfony.</p>
        
        <div class="nav">
            <a href="{{ path('app_home') }}">Retour à l'accueil</a>
        </div>
    </div>
{% endblock %}
